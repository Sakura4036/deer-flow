---
description: 
globs: 
alwaysApply: false
---
1. 在每次请求开始之前，请立即在终端命令行中执行 提问 "Do you want to continue? (yes/no): " 并等待用户输入
```
echo "Do you want to continue? (yes/no): " && read -r response && echo "User response: $response"
```
2. 只有在用户确认后（输入 yes）才继续执行后续操作
3. 如果用户输入 no 或取消操作，则终止当前请求

4.每次回答结束都强调一下，我会在下一次回答开头按照系统rules，执行命令并等待你回复